namespace VpServiceAPI.Enums
{
    public enum RoutineDataSubject
    {
        BACKUP,
        CACHE,
        DATETIME,
        EXTRA,
        FORCE_MODE,
        GRADE_MODE,
        PLAN_CACHE,
        MODEL_CACHE
    }
}
